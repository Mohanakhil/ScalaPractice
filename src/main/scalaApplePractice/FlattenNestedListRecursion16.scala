object FlattenNestedListRecursion16
{
//  val flatten_list: List[Any] => List[Int] = (list: List[Any]) => {
////    def flatten(list:List[Any]): List[Int] = {
////    if(list.isEmpty) list
////      if list.
////
////
////    }
////   flatten(list)
//  }

def main(Args:Array[String]):Unit={
  val nestedList=List(List(1,1),2,List(3,List(6,7)))
//  flatten_list(nestedList)
}
}
